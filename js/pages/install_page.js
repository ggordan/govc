/** @jsx React.DOM */

var React = require('react');

var InstallPage = React.createClass({

    // RENDER

    render: function() {
        return(
            <div id="app">
            </div>
        );
    },
});

module.exports = InstallPage;
